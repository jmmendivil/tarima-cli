#!/usr/bin/env node

var run = require('./'),
    path = require('path');

var pwd = process.cwd();

run({
  src: path.join(pwd, 'src/web'),
  dest: path.join(pwd, 'generated/dest/build'),
  cache: path.join(pwd, 'generated/dest/index.json'),
  watch: process.argv.indexOf('--watch') > -1,
  ignored: ['**/{images,sprites,fonts,icons*}/**'],
  exclude: ['!**/_*', '!**/_*/**'],
  bundle: true,
  rename: {
    coffee: 'js',
    less: 'css',
    jade: 'html'
  }
}, function(err) {
  if (err) {
    console.log(err.stack || err);
  }

  console.log('DONE');
});
